<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="270dp"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/toolbarColorBackground">

        <TextView
            android:id="@+id/lblTitleDialogAddReport"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/send_report"
            android:textColor="?attr/toolbarColorText"
            android:textSize="17sp"
            tools:ignore="HardcodedText" />
    </RelativeLayout>

    <EditText
        android:id="@+id/txtContentDialogAddReport"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:background="@drawable/background_edittext"
        android:gravity="right|top"
        android:hint="متن گزارش'"
        android:inputType="textMultiLine"
        android:padding="15dp"
        tools:ignore="HardcodedText" />

    <Button
        android:id="@+id/btnSubmitDialogReportAdd"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_marginTop="10dp"
        android:background="?attr/toolbarColorBackground"
        android:text="@string/send"
        android:textColor="?attr/toolbarColorText"
        android:textSize="17sp"
        tools:ignore="HardcodedText" />
</LinearLayout>